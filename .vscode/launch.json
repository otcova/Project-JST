{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "chrome",
            "url": "http://localhost:80/client-side",
            "webRoot": "${workspaceFolder}/client-side"
        }
    ]
}